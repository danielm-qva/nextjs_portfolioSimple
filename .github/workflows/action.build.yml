name: Nextjs

on:
  push:
   branches: [ "master" ]

jobs: 
    build: 
        name: "build App"
        runs-on: ubuntu-22.04
        steps:
          - uses: actions/setup-node@v3
            with:
              node-version: 16
          - run: 'node --version'
            
      